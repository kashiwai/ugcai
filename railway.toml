# Railway 設定ファイル (Railpack対応)
[build]
builder = "dockerfile"

[build.dockerfilePath]
# server/Dockerfile を使用
value = "server/Dockerfile"

[deploy]
startCommand = "node index.js"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
